# LibraryPro
📚 Library Pro — Desktop Library Management System

Современная библиотечная система на JavaFX + PostgreSQL, объединяющая удобную работу с каталогом книг, фильтрами, историей выдачи и базовой аналитикой.

⭐ 1. Чем наш проект отличается от обычного “списка книг”
1) 📊 Интерактивный Dashboard

количество всех книг

сколько доступно / выдано

диаграмма книг по жанрам (JavaFX PieChart / BarChart)

2) 🔍 Умный поиск и фильтры

поиск по названию и автору

фильтр по жанру

фильтр по статусу (Available / Borrowed)

3) 🧾 История выдачи

кто взял

дата выдачи

дата возврата

4) 😎 Единый современный дизайн

общий CSS-стиль

аккуратные кнопки, отступы, визуальная гармония

🎨 2. Структура экранов
LoginScreen

вход по логину/паролю

1 дефолтный пользователь в БД

DashboardScreen

карточки: "Всего книг", "Доступно", "Выдано"

диаграмма по жанрам

BooksScreen

таблица книг (название, автор, жанр, год, статус)

поиск и фильтры (TextField + ComboBox)

кнопки: Add, Edit, Delete

кнопки: выдача и возврат книги

BookFormDialog

создание / редактирование книги

поля: название, автор, жанр, год, описание

BorrowHistoryScreen

список всех операций по выбранной книге

borrower / borrow date / return date

🗄 3. Структура базы данных (SQLite)
books
поле	тип
id	INTEGER PK
title	TEXT
author	TEXT
genre	TEXT
year	INTEGER
status	TEXT (AVAILABLE, BORROWED)
borrow_history
поле	тип
id	INTEGER PK
book_id	INTEGER FK
borrower_name	TEXT
borrow_date	TIMESTAMP
return_date	TIMESTAMP (NULL, если не возвращено)
users (опционально)
поле	тип
id	INTEGER PK
username	TEXT
password	TEXT
🧩 4. Использование Java Collections

Проект использует коллекции для работы с каталогом:

ObservableList<Book> — для TableView

List<Book> — для данных из БД

Map<String, Long> — для группировки:

List<Book> allBooks = bookDao.getAll();
Map<String, Long> booksByGenre = allBooks.stream()
        .collect(Collectors.groupingBy(Book::getGenre, Collectors.counting()));


Это позволяет гибко фильтровать, сортировать и строить статистику.

📂 5. Структура проекта
library
 ├── Main.java
 │
 ├── controller
 │    ├── LoginController.java        // (опционально, но красиво)
 │    ├── DashboardController.java    // краткая статистика
 │    ├── BooksController.java        // список книг, фильтры, выдача/возврат
 │    └── BookFormController.java     // форма добавления/редактирования книги
 │
 ├── model
 │    ├── Book.java                   // книга
 │    ├── BorrowRecord.java           // запись о выдаче (для истории)
 │    ├── Reader.java                 // читатель (простой класс)
 │    └── Status.java                 // enum: AVAILABLE, BORROWED, OVERDUE
 │
 ├── dao
 │    ├── BookDao.java                // работа с таблицей books
 │    ├── BorrowRecordDao.java        // работа с таблицей borrow_history
 │    └── ReaderDao.java              // если делаете экран читателей
 │
 ├── service
 │    └── LibraryService.java         // вся бизнес-логика: выдача, возврат, поиск
 │
 ├── util
 │    ├── DbUtil.java                 // подключение к БД
 │    ├── DialogUtil.java             // алёрты/ошибки/подтверждения
 │    └── DateUtil.java               // работа с датами (опционально)
 │
 └── view
      ├── login.fxml                  // (опционально)
      ├── dashboard.fxml
      ├── books.fxml
      └── book_form.fxml




      library/
├── Main.java
├── controller/
│   ├── LoginController.java          # (опционально) экран логина
│   ├── DashboardController.java      # краткая статистика по библиотеке
│   ├── BooksController.java          # список книг, фильтры, выдача/возврат
│   └── BookFormController.java       # форма добавления/редактирования книги
├── model/
│   ├── Book.java                     # класс книги
│   ├── BorrowRecord.java             # запись о выдаче книги (для истории)
│   ├── Reader.java                   # класс читателя (простой)
│   └── Status.java                   # enum: AVAILABLE, BORROWED, OVERDUE
├── dao/
│   ├── BookDao.java                  # работа с таблицей books
│   ├── BorrowRecordDao.java          # работа с таблицей borrow_history
│   └── ReaderDao.java                # если делаете экран читателей
├── service/
│   └── LibraryService.java           # бизнес-логика: выдача, возврат, поиск
├── util/
│   ├── DbUtil.java                   # подключение к базе
│   ├── DialogUtil.java               # алёрты/подтверждения/ошибки
│   └── DateUtil.java                 # работа с датами (опционально)
└── view/
    ├── login.fxml                    # экран логина (опционально)
    ├── dashboard.fxml                # экран с краткой статистикой
    ├── books.fxml                    # основной экран с таблицей книг
    └── book_form.fxml                # форма добавления/редактирования книги

